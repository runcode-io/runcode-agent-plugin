interface:
  display_name: "RunCode SSH"
  short_description: "Connect Codex to RunCode workspaces over SSH."
  brand_color: "#0F766E"
  default_prompt: "Connect to my RunCode workspace"

policy:
  allow_implicit_invocation: true
